# WireMap — техническое задание на доработку

Версия v2. Основание: `docs/AUDIT.md` (аудит ревизии `b8b54cb`).
Статус: **объём согласован**, см. «Зафиксированные решения».

Этапы упорядочены по зависимостям: каждый следующий опирается на предыдущий.
Внутри этапа задачи независимы и могут делаться параллельно.

## Ход работ

| Задача | Статус |
|---|---|
| 0.6 Тесты и CI | **сделано** — тесты бэкенда (на август 2026 — 296), e2e на топологию в браузере, GitHub Actions на бэкенд, фронтенд и e2e |
| 0.1 Авторизация чтения | **сделано** — все маршруты кроме `/auth/login` и `/health` требуют токен, тест обходит их автоматически |
| 0.3 Fail-fast по конфигурации | **сделано** — `app/config.py`, отказ старта в продовом режиме |
| 0.4 Alembic и сведение схемы | **сделано** — нативные типы PostgreSQL, накат на уже развёрнутую базу проверен тестом |
| 0.5 Валидация входных данных | **сделано** — вместе с 0.4: раздельно между ними был бы промежуток, где кривой IP даёт 500 |
| 4.2 Развёртывание | **сделано** досрочно по запросу — весь стек одной командой |
| 0.2 Управление учётными записями | **сделано** — смена и сброс пароля, роли, блокировка, защита последнего админа; проверено в браузере |
| 2.6 Цвет устройства | **сделано** — цвет в шаблоне модели, красит узлы схемы |
| 2.7 Оформление узлов | **сделано** — карточка с цветной рамкой в духе Turbo Flow, без анимации |
| 2.3 Страница устройства | **сделано** — маршрут /devices/{id}, переход на схему с подсветкой |
| 2.4 Работа со схемой | **сделано** — связь мышкой, правка и удаление связи, добавление и удаление устройств прямо на схеме |
| 1.2 Стабильность раскладки | **частично** — схема больше не перестраивается на каждое изменение данных; Web Worker и Barnes–Hut остаются на потом |
| Порты из шаблона | **сделано** — состав портов задаёт модель, правка на устройстве только по флагу |
| Подвешенные концы связей | **сделано** — сняли сетевую карту, кабель остался; подключается заново к новому порту |
| Номер порта как идентификатор | **сделано** — номер обязателен и уникален в пределах устройства и модели, название — свободная подпись; миграция 0006 |
| Сплошная нумерация портов | **сделано** — номера идут 1..N без пропусков: номер раздаёт сервер, после удаления ряд смыкается |
| Кабель без концов | **сделано** — снятие последнего конца удаляет кабель, а не роняет сервер |
| Панель действий на узле схемы | **сделано** — править, копировать (новое устройство по той же модели), удалить прямо на топологии |
| Подключение повисшего конца мышью | **сделано** — заглушку тянут на устройство и выбирают гнездо; кабель остаётся тем же |
| Структура БД схемой | **сделано** — таблицы и стрелки внешних ключей в React Flow, раскладка по слоям |
| Вложенные группы (subflow) | **сделано** — цех — участок — линия, до трёх уровней; рамка внутри рамки, панель действий на рамке |
| Правка типов устройств | **сделано** — название и префикс правятся; коды заведённых устройств не переписываются |
| Разъёмы и модули | **сделано** — два справочника; разъём порта задаётся в модели, модуль вставляется у железки, пустая клетка видна |
| Режим порта | **сделано** — бывший «тип порта» переименован в режим и убран из шаблона: это настройка железки |
| Правка портов модели | **сделано** — название и разъём правятся в шаблоне и доезжают до всех её устройств; есть копия шаблона |
| Рамка группы как область | **сделано** — своё положение и размер (двигают и растягивают мышью, хранятся в базе), узлы не выходят за рамку, состав группы правится только явно |
| Порты только из шаблона | **сделано** — на странице устройства номер и название нельзя править, они читаются из модели |
| Отображение подвешенных концов | **сделано** — свободный конец кабеля рисуется на схеме заглушкой под устройством и считается занятым портом |
| Структура БД | **сделано** — вкладка «Структура БД», интроспекция живой базы: типы, ключи, ссылки, число строк |
| Прогон на ошибки | **сделано** — чистая установка, случайные операции с проверкой инвариантов, гонки, нагрузка 300 устройств; найдено и исправлено 9 дефектов (см. историю коммитов) |
| Настройки вида топологии | **сделано** — кнопка «Вид»: контур, заливка, скругление и подпись рамок групп, состав узла, толщина линий и подписи портов, фон; настройка личная, хранится в браузере. Заодно убрано штатное оформление React Flow для узлов типа `group`, из-за которого к каждой рамке прилипал лишний тёмный прямоугольник |
| Подписи портов на схеме | **сделано** — подпись рисуется поверх линии, а не под ней; сначала номер порта, потом название («№1 · Gi0/1»), название убирается в настройках вида |
| 2.2 Импорт устройств из файла | **сделано в облегчённом виде** — строки файла ложатся в промежуточную таблицу, оттуда переносятся по одной через обычное окно устройства; связи не импортируются |
| 0.8 Изоляция площадок | **сделано** — площадка у устройств, портов, кабелей, тегов, VLAN, групп и строк импорта; изоляция закреплена составными внешними ключами в PostgreSQL, переключатель в шапке, доступ раздаёт админ |
| 1.4 RBAC в интерфейсе | **сделано** — `viewer` не видит ни одной кнопки правки, поля портов только для чтения, схема не двигается; проверено обходом всех страниц: ни одного 403 |
| 1.3 Журнал изменений | **сделано** — `GET /audit` с фильтрами и постраничностью, страница «История» с разницей «было → стало», блок последних изменений в карточке устройства; логирование дописано в справочники |
| Схема на JointJS | **сделано** — второй вариант схемы связей, равный по возможностям первому: панели действий, кабели мышью, повисшие концы, группы с рамками, RBAC. Отличие — ортогональная разводка линий |
| 1.1 Пагинация и серверная фильтрация | **сделано** — список устройств лёгкий и страничный (порты подтягивает раскрытая карточка), поиск и отбор считает база; на 1000 устройств страница открывается за 1.3 с, первый ответ — 16 КБ |
| Порты в окне устройства | **сделано** — выбрал шаблон, и под формой сразу видно, какие порты получит устройство; при правке показаны его собственные порты с пометкой занятых |
| Заведение справочников из импорта | **сделано** — названия из файла, которым нет записи в базе, собраны в блок над таблицей импорта; «+» открывает обычное окно шаблона, группы или тега с подставленным названием. Раньше такое название молча терялось при переносе строки |
| Повторные строки в импорте | **сделано** — название и адрес, которые в спецификации уже есть, подсвечены зелёным и ведут на найденное устройство: файлы приносят повторно, и без пометки заводилась вторая такая же железка |
| Щелчок по схеме JointJS | **сделано** — панель узла и правка связи открывались с третьего-пятого раза: JointJS по умолчанию считает щелчком только тот, при котором мышь не сдвинулась ни на пиксель |
| Прыжок узлов на схеме JointJS | **сделано** — после перетаскивания рамки группы узлы возвращались на прежнее место, а масштаб схемы подгонялся заново на каждое изменение данных |
| Спецификация таблицей | **сделано** — устройства вместо карточек показаны таблицей с отбором под каждым заголовком (код, название, шаблон, тип, IP, расположение, группа, теги); условия складываются, считает их база. Порты по-прежнему подтягиваются только у раскрытой строки |
| Шаблоны по типам техники | **сделано** — вкладка «Шаблоны» сгруппирована по типу устройства раскрывающимися списками, у типа и у шаблона показано число заведённых устройств (`devices_count` считается по площадке одним запросом) |
| Шаблон из соседних окон | **сделано** — кнопка «Шаблон» на вкладке «Устройства» (как пресет кабеля на «Связях») и «Править шаблон» прямо в окне устройства: состав портов виден тут же, и уходить за правкой на другую вкладку незачем |
| Тёмная карточка узла | **сделано** — узел схемы больше не белый; вид переключается в «Виде» |
| Выбор схемы связей | **сделано** — выбран вариант на JointJS ради ортогональной разводки; схема на React Flow (`TopologyPage`, `DeviceNode`, `GroupNode`, `FloatingEdge`, `DanglingNode`, `actions.ts`) удалена вместе со вторым пунктом меню, `/topology-joint` уводит на `/topology`. React Flow остался только под «Структурой БД». Заодно фон полотна переехал на сетку JointJS, а настройка «Мини-карта» убрана: в `@joint/core` такого виджета нет |
| Схема в тёмной теме | **сделано** — подписи портов в рамке своего цвета, врезка подписи группы цветом полотна, карточка узла светлее фона: раньше всё это было рассчитано только на светлую тему |
| Название крупнее кода | **сделано** — на узле схемы крупно название железки, код мелкой строкой под ним: на схеме ищут «станок №7», а не «PLC-0002». Длинное название обрезается по месту многоточием |
| Кнопки панели при отдалении | **сделано** — панель действий держит экранный размер независимо от масштаба схемы; заодно колесо мыши масштабирует вокруг курсора, а не вокруг угла полотна |
| Перестановка конца кабеля | **сделано** — `POST /links/{id}/reconnect`, в окне связи оба конца выбираются из свободных портов: кабель записали не в тот порт — переставляется конец, а не заводится новая связь |
| R1 Транковые VLAN | **сделано** — отдельная таблица `interface_trunk_vlans` с составными ключами вместо массива чисел: чужой и несуществующий VLAN теперь отвергает сама база, а удаление VLAN убирает его из транков. Миграция 0014 переносит только настоящие значения |
| R2 История устройства из импорта | **сделано** — запись журнала получает идентификатор устройства; раньше заведение из импорта в истории самой железки не показывалось |
| R3 Заведение по шаблону в одном месте | **сделано** — `app/provisioning.py`; два маршрута (руками и переносом строки импорта) повторяли это дословно и уже разошлись |
| R4 Сверка счётчика кодов | **сделано** — при старте `code_sequences` подтягивается к фактическим кодам: после восстановления из дампа следующее устройство больше не упирается в занятый код |
| Структура БД на JointJS | **сделано** — вторая схема переехала на то же полотно, что и схема связей: React Flow удалён из зависимостей (бандл 1.41 → 1.24 МБ). Автоматическая раскладка по слоям осталась, к ней добавлены кнопки «Разложить» и «Вписать»; на схеме связей они тоже разделены |
| R5 Ограничение перебора паролей | **сделано** — после пяти промахов подряд вход по этой учётной записи закрывается на паузу, удваивающуюся до получаса; удачный вход счётчик стирает. Насовсем не запирает никогда: иначе достаточно поколотить чужой логин, чтобы человек не смог работать. Миграция 0015 |
| R8 Резервное копирование | **сделано** — `scripts/backup.sh` и `restore.sh` (плюс `.ps1` для Windows): копия снимается с работающей базы, хранятся последние N, восстановление спрашивает подтверждение и само снимает копию текущего состояния. Раздел в README, строка для cron |
| R7 / 1.6 Оптимистичная блокировка | **сделано** — у устройства, порта и связи есть номер правки; клиент присылает тот, что видел, и при расхождении получает 409 «запись изменена другим пользователем» вместо тихой перезаписи. Номер не двигают перетаскивание узла по схеме и сохранение без изменений — иначе соседу отбивало бы правку без причины. Миграция 0016 |
| R10 Типы фронтенда из OpenAPI | **сделано** — `backend/scripts/dump_openapi.py` выкладывает `openapi.json` без запуска сервера, `npm run codegen` превращает его в `src/api/schema.ts`, а `src/api/types.ts` стал списком псевдонимов над ним. Расхождение теперь ловит CI: бэкенд проверяет, что `openapi.json` не отстал от кода, фронтенд — что `schema.ts` не отстал от `openapi.json`. Ручными остались только параметры строки запроса: в OpenAPI они лежат не схемами, а списками у каждого маршрута |
| 0.7 Гигиена репозитория | **почти сделано** — удалён прототип на чистом JS, заменены устаревшие API (`lifespan`, `datetime.now(timezone.utc)`, `db.get`), корневой README и `.dockerignore` по контекстам сборки уже были. Остался `LICENSE` — выбор лицензии за владельцем |

Порядок задач 0.2 и 0.4 пришлось поменять местами относительно исходного
плана: 0.2 добавляет колонки в `users`, а без миграций такое изменение не
доезжает до уже развёрнутой базы — `create_all` меняет только отсутствующие
таблицы.

## Ревизия проекта (август 2026)

Сплошной разбор дерева после закрытия этапа 0 и большей части этапа 1.
Ниже — то, что нашлось, с оценкой и предлагаемым порядком. Пункты, уже
описанные в этапах 1–4, здесь не повторяются, а только приоритизируются.

### Что держит систему

Модель данных — сильная сторона проекта. Правила, которые в подобных
системах обычно живут в коде и постепенно расходятся с реальностью, здесь
закреплены самой базой: изоляция площадок — составными внешними ключами,
занятость порта — уникальными индексами на концах кабеля, порядок сторон
связи — `CHECK (a < b)`, состав портов — ссылкой на порт модели. Статус
порта нигде не хранится и потому не может рассинхронизироваться. Это
означает, что ошибка в приложении портит документацию, но не саму базу.

Второе — обозримость. 9 300 строк бэкенда и 8 200 фронтенда на 82 маршрута:
для системы такого объёма это мало, и читается всё за вечер. Слоёв нет
(роутер работает с ORM напрямую), и на этом размере это скорее плюс.

### Найденные дефекты

| # | Что | Почему это важно | Оценка |
|---|---|---|---|
| R1 | `interfaces.trunk_vlan_ids` — массив `int` без всяких проверок: ни внешнего ключа, ни валидации в API. Проверено на живом API: `PATCH` с `[999999]` принимается | Единственное место, где изоляция площадок не гарантирована базой: в trunk можно записать VLAN чужой фабрики или удалённый. Разъезжается тихо — увидят через полгода | S |
| R2 | Устройство, заведённое из импорта, пишется в журнал с `entity_id = NULL` | В истории самого устройства его создание не видно: блок «последние изменения» на карточке фильтрует по `entity_id` | XS |
| R3 | «Завести устройство по шаблону и материализовать его порты» продублировано в `routers/devices.py` и `routers/imports.py` слово в слово | Уже разошлось — в записи журнала (см. R2). Дальше разойдётся в правилах | S |
| R4 | `code_sequences` не сверяется с фактическими кодами при старте | После восстановления из дампа или ручной вставки следующий код упрётся в уникальный индекс, и заведение устройств встанет | XS |
| R5 | Вход не ограничен по частоте попыток | Периметр внутренний, но подбор пароля к учётке с ролью `admin` — самый дешёвый путь внутрь | S |
| R6 | Списки без постраничности: `/device-templates` (со всеми портами каждого), `/topology/devices`, `/tags`, `/vlans`, `/import/rows` | На целевом объёме первые два уже тяжёлые: схема тянет 1000 устройств со всеми портами (~3.3 с). Остальные — на вырост | M |
| R7 | Нет оптимистичной блокировки (раздел 1.6) | Двое правят одну железку — второй молча затирает первого. На заводе за одной базой сидят несколько человек | M |
| R8 | Резервного копирования нет ни скриптом, ни в `docker compose` | Единственный том с данными; потеря — это вся документация сети | S |
| R9 | Журнал изменений растёт без ограничений и показывает идентификаторы вместо названий («модель: 1») | Через год — таблица на миллионы строк и нечитаемые записи | S |
| R10 | `frontend/src/api/types.ts` — ручное зеркало `schemas.py` | Расхождение уже случалось при каждом добавлении поля; ловится только глазами | M |

### Предложения по архитектуре

**1. Тонкий доменный слой между роутерами и ORM.** Не «сервисы ради
сервисов»: речь о трёх правилах, которые уже размазаны по маршрутам —
заведение устройства по шаблону, поддержание ряда портов и правила кабеля
(занятость, порядок сторон, перестановка конца). Их место — `app/domain/`,
роутер остаётся разбором запроса и ответом. Это закрывает R3 и делает
осмысленным SNMP-опрос из этапа 4: воркер должен заводить связи теми же
правилами, что и человек, а не своей копией.

**2. Trunk-VLAN — таблицей, а не массивом.** `interface_trunk_vlans
(interface_id, vlan_id, site_id)` с составными внешними ключами. Тогда
правило «VLAN только своей площадки» держит база, как и все остальные, а
удаление VLAN само подчищает его из транков. Закрывает R1 в том же стиле,
в каком сделано всё вокруг.

**3. Типы фронтенда — из OpenAPI, а не руками.** ✅ сделано:
`openapi-typescript` в `npm run codegen`, проверка расхождения в CI.

**4. Схема связей собирается на сервере (раздел 1.2).** `/topology` отдаёт
готовые узлы и рёбра, `/topology/devices` исчезает. Сейчас браузер получает
всю площадку со всеми портами ради того, чтобы посчитать по ним подписи.

**5. Разделить `TopologyPage.tsx` (992 строки).** Три разные вещи в одном
файле: загрузка данных, раскладка и вся обвязка JointJS. Разумное деление —
`useJointPaper` (полотно и события), `buildGraph` (данные → ячейки),
страница (состояние окон). Файл давно стал местом, куда страшно лезть.

**6. Резать бандл по маршрутам.** Сейчас 1.4 МБ одним куском: JointJS нужен
только на схеме, React Flow — только на «Структуре БД». `React.lazy` на
эти два маршрута снимает с первой загрузки заметную часть.

**7. Эксплуатация.** Скрипт резервного копирования и восстановления рядом с
`scripts/start.sh` и строка в README; заголовки безопасности в nginx;
ограничение частоты попыток входа. Для внутренней сети этого достаточно.

**8. Тесты фронтенда.** Сейчас их нет вовсе — только три e2e-сценария на
схему. Vitest на то, где логика уже не тривиальна: раскладка, разбор строк
импорта, права. Не ради покрытия, а ради того, чтобы правки в этих местах
перестали требовать браузера.

### Предлагаемый порядок

| Очередь | Что | Почему сейчас |
|---|---|---|
| 1 | R1 (trunk-VLAN таблицей), R2, R3, R4 | Дефекты данных. Чем позже, тем больше записей придётся чинить руками |
| 2 | R8 (резервные копии), R5 (частота входов) | Дёшево и снимает худшие сценарии эксплуатации |
| 3 | R7 (раздел 1.6, оптимистичная блокировка) | Нужен до того, как за базой сядут несколько человек |
| 4 | ~~R10 (типы из OpenAPI)~~, предложение 5 (деление `TopologyPage`) | Скорость всех дальнейших правок |
| 5 | Раздел 1.2 + R6 (постраничность, серверная схема) | Делать под реальный объём, а не заранее: сейчас 1000 устройств открываются за 1.3 с |
| 6 | R9 (журнал: названия вместо id, срок хранения) | Ценность растёт вместе с накопленной историей |
| 7 | 2.8 вложения, этап 3 (стойки, IPAM), этап 4 (SNMP/LLDP) | Новая функциональность — после того, как перечисленное закрыто |


## Зафиксированные решения

| Вопрос | Решение | Следствие для ТЗ |
|---|---|---|
| Целевой объём | **до 1000 устройств** (~24 000 портов при 24 портах на устройство) | Этап 1 обязателен: без пагинации список устройств станет неработоспособным задолго до этой отметки |
| Периметр | **только внутренняя сеть** | 2FA, аудит входов и короткий срок сессии не делаем. Авторизация чтения (0.1) остаётся обязательной: внутренняя сеть — не то же самое, что доверенная |
| Патч-панели и кабельные трассы | **не требуются** | Раздел 3.1 исключён из объёма |
| Экспорт (XLSX / кабельный журнал / схема в PDF) | **не требуется** | Раздел 2.1 исключён из объёма |
| Одновременная работа нескольких пользователей | **нужна, если не усложняет** | Раздел 1.6 включён в лёгком варианте: `version` + 409 при конфликте, без блокировок и слияния |
| SNMP/LLDP-опрос | **планируется** | Этап 4 остаётся в плане. Поля `links.source` / `confirmed` / `last_seen_at` сохраняем и из интерфейса не убираем (пункт D7 аудита закрыт этим решением) |
| Импорт из XLSX/CSV | **нужен** | Раздел 2.2 в объёме. Это основной путь первичного наполнения: 1000 устройств руками через интерфейс не вводятся |
| Разные фабрики | **полностью изолированные площадки** | Новый раздел 0.8 в фундаменте: ключ площадки в модели + гарантия на уровне PostgreSQL. Отдельные базы отвергнуты — та же изоляция ценой N установок |
| Цвет устройства | **задаётся в шаблоне устройства** | Раздел 2.6: `device_templates.color`, одна настройка на модель техники. Переопределения на конкретном устройстве не делаем |

---

## Этап 0. Фундамент — без него нельзя в продуктив

Цель: закрыть дыры, из-за которых систему нельзя показывать за пределами
локальной машины, и получить возможность безопасно менять код дальше.

### 0.1. Закрыть чтение авторизацией `[S1, P0]`

Все эндпоинты, кроме `POST /auth/login` и `GET /health`, требуют валидный
токен.

- Навесить `Depends(get_current_user)` на все GET-хендлеры (или подключить
  роутеры с `dependencies=[Depends(get_current_user)]` в `main.py` — так
  надёжнее: новый эндпоинт не забудут защитить).
- `/docs` и `/openapi.json` — закрыть или оставить открытыми осознанно
  (решение зафиксировать в README).

**Приёмка:** запрос `GET /devices` без заголовка `Authorization` → 401.
Тест на каждый роутер, что неаутентифицированный запрос отбит.

### 0.2. Управление учётными записями `[S2, P0]`

- `POST /auth/me/password` — смена своего пароля (старый + новый, проверка старого).
- `PATCH /auth/users/{id}` — смена роли и ФИО (только admin).
- `POST /auth/users/{id}/password` — сброс пароля пользователя (только admin).
- `DELETE /auth/users/{id}` — блокировка или удаление (решить: мягкое
  `is_active=false` предпочтительнее — `audit_log.user_id` ссылается на users).
- Запрет удалить/разжаловать последнего admin.
- Политика пароля: минимум 12 символов, проверка при создании и смене.
- 2FA и аудит попыток входа не делаем — система живёт во внутренней сети.
- Экран «Пользователи»: колонка действий, модалка смены пароля, пункт
  «Сменить пароль» в собственном профиле в сайдбаре.
- Флаг `must_change_password` у бутстрап-администратора: до смены пароля
  показывать баннер (или принудительный редирект на смену).

**Приёмка:** после первого входа под дефолтным admin интерфейс требует
сменить пароль; сменённый пароль работает, старый — нет.

### 0.3. Fail-fast по конфигурации `[S3, S5, P0]`

- Перевести настройки на `pydantic-settings` (уже в зависимостях, но не
  используется): `Settings(secret_key, database_url, cors_origins, environment, ...)`.
- При `ENVIRONMENT=production` и `secret_key` из списка дефолтных — падать
  на старте с внятным сообщением.
- `CORS_ORIGINS` по умолчанию — список конкретных origin, не `*`;
  `allow_credentials` выставлять только вместе с явным списком.
- `.env.example` дополнить всеми переменными с комментариями.

**Приёмка:** `docker compose up` без `SECRET_KEY` в проде не поднимается,
в dev — поднимается с предупреждением.

### 0.4. Alembic и сведение схемы `[D1, D2, P0]`

- Подключить Alembic, `alembic revision --autogenerate` от текущего состояния
  как baseline.
- **Привести `models.py` к `schema.sql`** (SQL-версия — правильная):
  `INET` для `devices.management_ip`, `interfaces.ip`, `vlans.gateway`;
  `CIDR` для `vlans.subnet`; `MACADDR` для `interfaces.mac`;
  `DATE` для `devices.install_date`; добавить `CHECK (interface_a_id < interface_b_id)`.
  Использовать `sqlalchemy.dialects.postgresql.INET/CIDR/MACADDR`.
- Убрать `create_all()` из старта; в контейнере — `alembic upgrade head` до
  запуска uvicorn.
- Сидирование справочника типов устройств и бутстрап-админа перенести в
  отдельную идемпотентную команду/миграцию данных.
- `schema.sql` оставить как справочную документацию с пометкой, что источник
  истины — миграции (или удалить, чтобы не расходилось второй раз).

**Приёмка:** база, поднятая с нуля миграциями, совпадает по DDL с `schema.sql`
(проверяется тестом-сравнением через `pg_dump --schema-only` или
`alembic check`).

### 0.5. Валидация входных данных `[D3, S9]`

- `IPvAnyAddress` для IP, отдельный валидатор MAC (нормализация в один
  формат), `datetime.date` для `install_date`, `IPvAnyNetwork` для `subnet`.
- `port_type`, `role`, `media_type`, `line_style`, `source` — перевести на
  `Literal`/`Enum` в схемах (сейчас `str` + CHECK только в БД).
- `source` и `confirmed` убрать из `LinkCreate` (выставляются сервером:
  ручное создание → `manual`/`true`; поля остаются под будущий импорт SNMP).
- Ограничения длины на текстовые поля.

**Приёмка:** кривые IP/MAC/дата → 422 с понятным сообщением, не 500.

### 0.6. Тесты и CI `[E1, E2, P0]`

- Backend: `pytest` + `httpx.AsyncClient` + Postgres в CI (services или
  `testcontainers`). Покрыть: авторизацию и роли на каждом роутере,
  генерацию кодов (в т.ч. параллельную), создание/удаление связи и
  пересчёт статуса порта, каскады (удаление устройства/тега/шаблона),
  циклы в тегах, дубликаты label.
- Frontend: `vitest` + Testing Library на утилиты (`flattenTagsOrdered`,
  `tagPath`, `nn*`) и раскладку топологии; smoke-рендер страниц с
  замоканным API.
- GitHub Actions: `ruff` + `mypy` (или хотя бы `ruff`) + `pytest` для
  backend; `tsc -b` + `oxlint` + `vitest` + `vite build` для frontend.
  Обязательные проверки на PR.

**Цель покрытия:** backend ≥ 70 % по строкам, критичные пути (auth, links,
codegen) — 100 %.

### 0.7. Гигиена репозитория `[E5, E6, E7]` — **почти сделано**

- ~~Корневой `README.md`~~ — есть: что за продукт, для кого, быстрый старт,
  ссылки на `backend/README.md`, `frontend/README.md`, `docs/AUDIT.md`,
  `docs/TZ.md`. Скриншотов пока нет.
- ~~Удалить `frontend-vanilla-prototype/`~~ — удалён, история в git.
- ~~`.dockerignore`~~ — свой у каждого контекста сборки (`backend/`,
  `frontend/`); корневого не нужно, из корня ничего не собирается.
  ~~Пароль Postgres из переменной окружения~~ — берётся из `.env`, без
  значения по умолчанию: без него `docker compose` не стартует.
- ~~Заменить устаревшие API `[E4]`~~ — сделано: `lifespan` вместо
  `on_event`, `datetime.now(timezone.utc)` вместо `utcnow()`,
  `db.get(Model, id)` вместо `query().get()`. Предупреждений при прогоне
  тестов больше нет.
- **Осталось:** `LICENSE` — выбор лицензии за владельцем репозитория, сам
  файл добавляется в одну строку.

---

### 0.8. Изоляция площадок (разные фабрики)

Требование: схемы разных фабрик не пересекаются никак. Это свойство модели
данных, а не настройка отображения, поэтому задача стоит в фундаменте.

**Почему именно сейчас, а не в этапе 2.** Ключ площадки добавляется в
`devices` и ещё пяток таблиц. Сегодня в базе три устройства — миграция
мгновенная. После импорта тысячи устройств придётся переразмечать их руками,
а сам импорт должен уметь колонку «Площадка» с первого дня. Дешевле сделать
до наполнения.

#### Почему не отдельные базы

Соблазн понятный, но цена высокая: N установок обслуживать, обновлять и
бэкапить по отдельности; справочник моделей техники дублировать в каждой;
сквозного поиска и общего управления пользователями нет вовсе. Изоляцию той
же прочности даёт ключ площадки с ограничениями на уровне PostgreSQL — база
физически не сможет хранить связь между фабриками (см. ниже).

Отдельные установки оправданы в одном случае: если у фабрик разные
администраторы, которые не должны видеть чужие данные даже теоретически, и
разные регламенты обслуживания. Тогда это та же система, развёрнутая
несколько раз, и решение можно принять когда угодно потом — код один и тот же.

#### Что изолируется, а что общее

| По площадкам | Общее на всю систему |
|---|---|
| `devices`, `interfaces`, `links` | `device_types` — категории |
| `topology_groups`, позиции узлов | `device_templates` и их порты — модели техники |
| `tags` — «Цех 1» есть на каждой фабрике | `link_templates` — пресеты кабелей |
| `vlans` — номера у фабрик свои и совпадают | `users` — люди могут работать с несколькими площадками |

Логика деления: описание конкретной сети принадлежит площадке, справочник
моделей техники — общий. Заводить «Cisco Catalyst 2960» заново на каждой
фабрике смысла нет.

**Уникальности переопределяются** там, где раньше были глобальными:
`vlans (site_id, vlan_number)`, `tags (site_id, parent_id, name)`,
`topology_groups (site_id, name)`.

`devices.code` предлагается оставить **глобально уникальным** — общий счётчик,
сквозная нумерация. Так «упал SW-0042» звучит однозначно и сквозной поиск
даёт один ответ. Альтернатива — своя нумерация на каждой площадке, тогда
SW-0001 будет на каждой фабрике; решается одной строкой в генераторе кодов,
но требует явного решения.

#### Гарантия на уровне базы

Проверок в приложении мало: ошибка в одном запросе — и связь уедет между
фабриками. Поэтому изоляция закрепляется составными внешними ключами:

```sql
-- денормализованный site_id + составная уникальность
ALTER TABLE devices    ADD UNIQUE (id, site_id);
ALTER TABLE interfaces ADD COLUMN site_id INT NOT NULL,
                       ADD UNIQUE (id, site_id),
                       ADD FOREIGN KEY (device_id, site_id) REFERENCES devices(id, site_id);
ALTER TABLE links      ADD COLUMN site_id INT NOT NULL,
                       ADD FOREIGN KEY (interface_a_id, site_id) REFERENCES interfaces(id, site_id),
                       ADD FOREIGN KEY (interface_b_id, site_id) REFERENCES interfaces(id, site_id);
```

После этого связь между портами разных площадок нельзя записать в принципе —
не поможет ни ошибка в коде, ни ручной INSERT.

#### Доступ и интерфейс

- Таблица `user_sites(user_id, site_id)` — кому какие площадки доступны;
  роль `admin` видит все.
- Переключатель площадки в шапке; всё приложение работает в контексте
  выбранной. Выбранная площадка — в адресе страницы, чтобы схему можно было
  послать ссылкой.
- Все списки и поиск фильтруются по площадке. Запрос чужого объекта по
  прямому id — 404, а не 403: чужие данные не должны даже подтверждать своё
  существование.

**Приёмка:** пользователь с доступом к одной площадке не видит ни устройств,
ни связей, ни тегов другой ни в списках, ни поиском, ни по прямой ссылке;
попытка создать связь между портами разных площадок отвергается базой (тест
делает это в обход приложения, прямым INSERT).

#### Сделано

Всё перечисленное выше — с тремя уточнениями по реализации.

**Площадка едет заголовком `X-Site-Id`, а не параметром каждого маршрута.**
Она относится ко всему, что человек сейчас делает, как язык или часовой
пояс, и выбирается один раз в шапке. Когда площадка одна (обычный случай
только что развёрнутой системы), заголовок можно не слать — сервер
подставит её сам, и все прежние вызовы API продолжают работать. В адресе
страницы площадка тоже есть (`?site=2`), чтобы схему можно было послать
ссылкой.

**Требуется PostgreSQL 15+.** Составные ключи объявлены с
`ON DELETE SET NULL (колонка)`: без перечисления колонки снятие порта
обнуляло бы вместе со ссылкой и площадку кабеля, а она `NOT NULL`. В
docker-compose и так стоит 16.

**Связка «устройство — тег» проверяется приложением, а не ключом.**
Добавить в `device_tags` третью колонку с площадкой мешает ORM: строки этой
таблицы пишет SQLAlchemy через отношение и заполнить лишнюю колонку не
может. Практический риск невелик — чужой тег на устройстве не соединяет
сети, а лишь испортил бы список, — но это единственное место изоляции без
гарантии на уровне базы. То же касается родителя у тега и у группы
топологии: там проверка тоже прикладная.

Проверено 11 тестами (`tests/test_sites.py`), из них два бьют прямо в базу
мимо приложения: кабель между площадками и перенос порта на чужое
устройство отвергаются. Отдельный тест в `test_migrations.py` проверяет
накат на уже наполненную базу: данные уезжают на одну общую площадку, а
заведённые люди сохраняют к ней доступ.

## Этап 1. Готовность к реальному объёму данных

Цель: система остаётся отзывчивой на целевом объёме — **1000 устройств,
~24 000 портов, порядка 10 000 связей**. Это не запас на будущее: при текущей
архитектуре (всё грузится одним запросом) деградация начнётся уже на
двух-трёх сотнях устройств.

### 1.1. Пагинация и серверная фильтрация `[P1, P2]` — **сделано**

Измерено на сгенерированной базе: 1000 устройств, 19 500 портов, 6500
кабелей (`python -m scripts.generate_data --devices 1000`).

| | было | стало |
|---|---|---|
| `GET /devices` | 3.3 с, 7.6 МБ | 0.10 с, 16 КБ |
| Страница «Устройства» в браузере | не открывалась осмысленно | 1.3 с |
| `GET /links` | всё разом | 0.03 с на 100 строк |

Что изменилось:

- `GET /devices` отдаёт `{items, total}` с **лёгкими** записями: вместо
  вложенных портов два числа — всего и занято. Порты подтягивает раскрытая
  карточка (`GET /devices/{id}/interfaces`). Порты и составляли почти весь
  вес ответа: на целевом объёме это двадцать тысяч вложенных объектов ради
  пятидесяти строк на экране.
- Поиск (`q` по коду, имени, расположению и IP), отбор по тегу, типу, модели
  и группе, сортировка и `limit`/`offset` — на сервере. Фильтровать тысячу
  записей в браузере можно, только сначала их туда привезя.
- `GET /links` тоже страничный и отдаёт концы кабеля сразу с подписями
  (`end_a`, `end_b`): раньше страница связей держала в памяти все устройства
  со всеми портами только ради того, чтобы вместо «интерфейс 4312» написать
  «SW-0003 · №2 Gi0/2».
- `GET /interfaces/free` — свободные порты для списка «куда воткнуть»: их
  тоже ищет база.
- Схеме связей нужны все порты площадки сразу, страницами её не покажешь —
  у неё свой маршрут `GET /topology/devices`. Это временно: когда схема
  научится собираться на сервере (1.2), маршрут уйдёт вместе с ней. Сейчас
  он стоит 3.3 с на тысяче устройств, и это единственное оставшееся тяжёлое
  место.
- Наполнение тестовой базы — `backend/scripts/generate_data.py`
  (`--devices`, `--links`, `--clean`), входило в объём задачи.

Исходная постановка:

- `GET /devices`: `limit`/`offset` (или cursor), `q` (поиск по code/name/IP),
  сортировка; ответ `{items, total}`.
- Лёгкий режим списка: `DeviceListItem` без вложенных интерфейсов
  (`ports_total`, `ports_connected` числами) — интерфейсы подгружаются
  только для раскрытой карточки через `GET /devices/{id}/interfaces`.
- Аналогично `GET /links` (+ фильтр по устройству), `GET /audit`.
- Фронтенд: перевести фильтры тегов/типов на серверные параметры,
  бесконечная прокрутка или постраничная навигация.

**Приёмка:** страница «Устройства» на целевом объёме (1000 устройств,
24 000 портов) открывается < 1.5 с, первый ответ API < 200 КБ. Проверяется на
сгенерированном наборе данных — скрипт наполнения тестовой базы входит в
объём задачи.

### 1.2. Топология: производительность и стабильность `[P3, P6]`

- Раскладку считать один раз при первой загрузке набора узлов, а не на
  каждое изменение данных; при мутациях — обновлять узлы точечно, не
  перекладывая схему.
- Вынести симуляцию в Web Worker либо ограничить итерации/включить
  Barnes–Hut при n > 100.
- Кнопка «Переразложить» (сбросить сохранённые позиции для видимого набора)
  + `DELETE /devices/{id}/position` или `PATCH` с `null`.
- Решить судьбу `GET /topology`: либо фронтенд переходит на него (тогда
  сервер отдаёт готовые узлы/рёбра и фильтрацию), либо эндпоинт удаляется.
  **Рекомендация:** перейти на серверный `/topology` — он уже отдаёт ровно
  то, что нужно схеме, и снимает необходимость грузить все устройства целиком.
- Фильтры схемы: по тегу (есть), по группе, по типу устройства, скрытие
  неподключённых узлов.

**Приёмка:** 300 узлов / 400 связей — открытие схемы < 2 с, перетаскивание
без лагов, после правки связи схема не «прыгает».

### 1.3. Журнал изменений `[D4]` — **сделано**

Как сделано:

- `GET /audit` — фильтры по сущности, её id, автору и периоду, постраничность
  (`{items, total}`). Доступен **всем ролям**, включая `viewer`: кто и что
  менял — рабочая информация, и спрашивает об этом обычно как раз тот, кто
  сам править не может.
- Разницу «было → стало» считает сервер, а не интерфейс: сравнивать два
  снимка столбцов — работа над данными, и делать её в каждом клиенте заново
  незачем. Поля переведены на русский, служебные (`updated_at`, хеш пароля,
  координаты узла) в разницу не попадают.
- Правка, которая ничего не изменила, в журнал не пишется вовсе: форма
  устройства шлёт свойства и теги двумя запросами, и один из них обычно
  пустой — такая запись только сбивала бы с толку.
- Записи площадки видны на своей площадке, записи об общих справочниках и о
  людях — везде. Без этого журнал обходил бы изоляцию: по нему были бы видны
  чужие устройства.
- Логирование дописано туда, где его не было: теги, VLAN, типы устройств,
  разъёмы, модули, шаблоны связей, группы топологии, площадки. Перетаскивание
  узлов по-прежнему не логируется — это оформление схемы.
- Интерфейс: страница «История» (фильтры, постраничность) и блок «Последние
  изменения» в карточке устройства.

Осталось грубым: ссылки на другие записи показываются числами («модель: 1»,
«конец A: 5») — подставлять их названия значит на каждую строку журнала
лезть в другие таблицы, и делать это стоит вместе с 1.1, где такие выборки
всё равно придётся собирать пачками.

Исходная постановка:

- `GET /audit` с фильтрами (сущность, id, пользователь, период) + пагинация,
  только admin (или admin+editor — согласовать).
- Дописать логирование там, где его нет: теги, VLAN, типы устройств,
  шаблоны связей, группы топологии, пользователи, позиции узлов — не логировать.
- Страница «История»: таблица + диф старого/нового значения.
- Блок «последние изменения» в карточке устройства.

**Приёмка:** любое изменение справочника видно в журнале с автором и временем.

### 1.4. RBAC в интерфейсе `[S8]` — **сделано**

Право всего два — `edit` и `admin`, ровно как на сервере; проверяются хуком
`useCan`, а не разбором роли по месту. Прятать кнопку, за которой стоит 403,
— не защита (защищает сервер), а честность интерфейса: кнопка, которая
заведомо не сработает, обманывает. У `viewer` поля портов показываются, но
не правятся, схема не двигается и кабель не тянется, панели действий на
узлах нет.

Приёмка проверена обходом всех страниц под `viewer`: ни одного ответа 403.

Исходная постановка:

- `viewer` не видит кнопок создания/правки/удаления; поля форм — read-only.
- `editor` не видит раздел «Пользователи» (уже так) и системных справочников,
  отданных admin.
- Централизованный хук `useCan('device:edit')` вместо проверок по месту.

**Приёмка:** под `viewer` в интерфейсе нет ни одного действия, приводящего к 403.

### 1.5. Сессии и 401 `[S7]` — **сделано**

- ~~`apiFetch`: 401 → сброс токена, редирект на `/login` с сообщением
  «сессия истекла», без каскада красных тостов.~~ — сделано.
- Продление токена при активности либо refresh-токен (согласовать: для
  внутренней системы допустимо просто увеличить TTL и корректно обрабатывать
  истечение).

### 1.6. Прочность записи `[D5, D6, E10, P5]` — **в основном сделано**

- ~~Уникальность связи на уровне БД: канонический порядок `a < b` +
  перекрёстное ограничение~~ — сделано.
- ~~Синхронизация `code_sequences` при старте~~ — сделано (R4).
- ~~Bulk-создание портов: `POST /devices/{id}/interfaces/bulk`~~ — сделано.
- `InterfaceRow`: сброс локального состояния при изменении входных данных
  (`key` по `updated_at`) либо переход на управляемую форму.
- ~~Оптимистичная блокировка на устройстве, порте и связи~~ — **сделано**:
  целочисленный `version`, клиент присылает его в `PATCH`, при расхождении —
  409 и сообщение «запись изменена другим пользователем». Ни блокировок, ни
  слияния изменений нет, как и договаривались. Две оговорки по ходу дела:
  номер не двигают перетаскивание узла по схеме (это не правка карточки) и
  сохранение, ничего не изменившее, — иначе у соседа, правящего ту же
  запись, сохранение отбивалось бы без всякой причины. Интерфейс на 409 сам
  подтягивает свежие данные, чтобы человек сразу видел чужую правку.
- **Осталось:** `InterfaceRow` — сброс локального состояния при изменении
  входных данных (`key` по `updated_at`).

**Приёмка:** два браузера открывают один порт, первый сохраняет — второй при
сохранении получает понятное сообщение вместо тихой перезаписи.

---

## Этап 2. Функциональность, которой не хватает ежедневно

### 2.1. Экспорт — ~~в объём не входит~~

Исключено решением заказчика. Пункт 3.4 аудита (нет выгрузки спецификации,
кабельного журнала, схемы в PDF) закрывается как «не требуется», а не как
незакрытый долг. Если понадобится позже — вернуть отдельной задачей,
техническое препятствий нет.

### 2.2. Импорт из XLSX/CSV

Основной путь первичного наполнения — через него в систему заедут те самые
1000 устройств. Поэтому раздел проработан подробнее остальных в этапе 2 и
по факту нужен раньше прочего (см. «Порядок работ» ниже).

#### Сделано: перенос через промежуточную таблицу

Заказчик выбрал вариант проще описанного ниже, и он сделан целиком.

Файл **не заводит устройства сам**. Строки читаются в отдельную таблицу
`import_rows` и там же остаются: спецификация оборудования до переноса не
меняется ни на запись. На вкладке «Импорт» видно, что разобрано из файла, а
кнопка «+» в строке открывает обычное окно устройства с подставленными
данными — шаблон, название, IP, расположение, группа, теги. Человек
дозаполняет, чего в файле не было, и нажимает «Создать»; строка помечается
«перенесена» и получает ссылку на заведённое устройство.

Почему так, а не «применение одной транзакцией»: в принесённых файлах
строки неполные и с опечатками, и проверять их пришлось бы всё равно
глазами. Промежуточная таблица делает эту проверку рабочим состоянием, а не
циклом «загрузил — получил список ошибок — правлю файл — загружаю снова».

Детали реализации:

- **Разбор нестрогий.** Заголовки опознаются по синонимам («Наименование»,
  «Имя», «name», «hostname» — одно поле), сравнение без регистра, пробелов и
  дефисов. Колонки, которым нет места в модели (инвентарный номер,
  серийник), не выбрасываются, а складываются в `extra` и при переносе
  дописываются в заметки: по ним человек и опознаёт железку.
- **Форматы:** `.xlsx`/`.xlsm` (openpyxl) и `.csv`/`.txt`. Разделитель
  определяется сам (`,`, `;`, таб), кодировка перебирается
  utf-8-sig → utf-8 → cp1251 — Excel на русской локали сохраняет именно так.
- **Неполные строки принимаются.** Пустая строка пропускается, строка с
  одним названием — нет.
- **Шаблон устройства, группа и теги** ищутся в справочниках по названию без
  учёта регистра; ненайденное название не ошибка, а пометка «нет в базе» —
  выбрать нужное человек может прямо в окне переноса.
- **Чего в справочниках нет — заводится с той же страницы.** Названия, не
  нашедшие себе записи, собраны в блок над таблицей: шаблоны, группы, теги.
  «+» открывает обычное окно справочника с подставленным названием — то же,
  что на вкладках «Шаблоны», «Топология» и «Теги»: своей упрощённой формы
  здесь нет, иначе шаблон заводился бы в двух местах по-разному. Без этого
  блока связь строки со справочником молча терялась: поле в окне переноса
  оставалось пустым, и заметить это по таблице было нельзя. После заведения
  подсказки в строках пересчитываются сразу.
- **Строка, которая уже есть в базе, помечена.** Название и адрес, совпавшие
  с заведённым устройством, подсвечены зелёным и ведут на него. Файлы
  приносят повторно — с доливкой пары строк или после правки, — и без
  пометки человек заводил вторую такую же железку. Запрета нет: настоящие
  тёзки бывают, решение остаётся за человеком.
- **Перенос идёт через `/import/rows/{id}/move`,** а не через обычное
  создание устройства: устройство и пометка строки записываются одной
  транзакцией, повторный перенос той же строки — 409, а не второе
  устройство. Порты приезжают из модели, как у любого заведённого руками.
- **Ограничения:** 16 МБ и 10 000 строк на файл; загрузка требует роли
  `editor`.
- **Связи не импортируются** намеренно: кабель — это пара портов, а портов
  у устройства до его заведения ещё нет.

Проверено: 10 тестов бэкенда (разбор, неполные строки, xlsx, cp1251,
нечитаемый файл, перенос и повтор, права) и прогон в браузере — загрузка
csv, окно с подставленными данными, устройство в спецификации.

#### Не делалось из описанного ниже

Сопоставление колонок мышью и его запоминание, построчный предпросмотр с
валидацией, применение одной транзакцией на весь файл, режимы повторной
загрузки, выгрузка отчёта, импорт шаблонов и связей. Развёрнутый вариант
сохранён как есть — если понадобится грузить файлы тысячами строк без
ручного разбора, возвращаться к нему.

#### Развёрнутый вариант (не реализован)

**Сценарий.** Загрузка файла → сопоставление колонок файла с полями системы
(с запоминанием сопоставления для повторных загрузок) → предпросмотр с
построчной валидацией и подсветкой проблем → применение одной транзакцией
(или ничего, или всё).

**Что импортируем:**
- **Шаблоны устройств** — модель, производитель, категория, список портов.
  Первыми: устройства на них ссылаются.
- **Устройства** — шаблон (по названию), название, IP управления,
  расположение, роль, дата установки, теги, группа на топологии.
  `code` генерируется системой и в файле не участвует.
- **Связи** — устройство A, порт A, устройство B, порт B, шаблон связи,
  разъём, длина. Отдельным файлом, после устройств.

**Разрешение ссылок.** В файле люди пишут названия, а не id: шаблон, теги,
группа, шаблон связи, устройства в связях ищутся по названию/коду.
Ненайденное — ошибка строки с внятным текстом («шаблон "Cisco 2960" не
найден»), а не молчаливый пропуск. Отсутствующие теги по галочке
«создавать недостающие теги» могут заводиться на лету.

**Валидация строк** переиспользует схемы Pydantic из 0.5 — IP, MAC, дата,
перечисления проверяются одинаково и при ручном вводе, и при импорте.

**Отчёт:** создано / обновлено / пропущено, и построчный список ошибок с
номерами строк исходного файла. Отчёт выгружается, чтобы можно было
исправить файл и загрузить повторно.

**Повторная загрузка.** Режим на выбор: «только добавлять» (существующие
пропускать) или «добавлять и обновлять» (сопоставление по коду устройства).
Удаление импортом не делается никогда.

**Приёмка:** файл на 1000 строк с десятком намеренно битых строк —
предпросмотр показывает ровно эти строки с причинами, применение либо
проходит целиком, либо не меняет базу вовсе; повторная загрузка того же
файла в режиме «только добавлять» не создаёт дублей.

### 2.3. Карточка устройства и навигация

- Маршрут `/devices/{id}` — отдельная страница: свойства, порты, связи,
  история, соседи по схеме, кнопка «показать на схеме».
- Глобальный поиск (Ctrl+K) по коду, имени, IP, MAC, серийнику,
  расположению, тегу — с переходом на найденное.
- Расширить `GET /search`: сейчас ищет только по `ip/mac/name/code`,
  добавить заметки, расположение, теги, модель; отдавать тип найденного.

### 2.4. Работа со схемой

- Создание связи перетаскиванием между узлами (`onConnect` → выбор портов
  на обоих концах → `POST /links`).
- Удаление/правка связи по клику на ребро.
- Назначение группы перетаскиванием узла в рамку.
- Легенда (цвета шаблонов связей), автоподбор масштаба под печать.

### 2.5. Изоляция площадок — ПЕРЕНЕСЕНО В ЭТАП 0, см. раздел 0.8

Требование «схемы не пересекаются никак» — это не настройка отображения, а
свойство модели данных, поэтому раздел переехал в фундамент. Причины и
устройство — в разделе 0.8.

### 2.6. Цвет устройства

Сейчас цвет есть у тега, у шаблона связи и у группы на топологии, а у самого
устройства — нет, все узлы схемы одинаковые.

**Цвет задаётся в шаблоне устройства** (`device_templates.color`), то есть на
уровне модели техники: все «Cisco Catalyst 2960» одного цвета, все «ЧПУ Haas
VF-2» — другого. Заводится один раз вместе со шаблоном, красить устройства
поштучно не требуется.

- На схеме цвет уходит в рамку и заголовок узла.
- В списке устройств — в маркер строки.
- Пустое значение — узел рисуется нейтральным, как сейчас.

Переопределение цвета на конкретном устройстве не делаем: до появления
реальной потребности это лишнее поле в форме. Если понадобится — добавляется
позже колонкой `devices.color`, ничего не ломая.

### 2.7. Оформление узлов в стиле Turbo Flow

Turbo Flow — это пример оформления из галереи React Flow (той же библиотеки,
на которой схема уже построена), а не отдельная библиотека: тёмная карточка,
анимированная градиентная рамка, иконка слева, заголовок и подзаголовок.
Переход на него — переписывание `DeviceNode.tsx` и стилей рёбер, модель
данных и логика раскладки не затрагиваются.

Две оговорки, которые стоит учесть до начала:

- оформление рассчитано на тёмный фон; в светлой теме градиентные рамки
  выглядят заметно хуже, так что либо схема всегда тёмная, либо нужен второй
  набор цветов;
- анимированные рамки — это CSS-анимация на каждом узле. На целевых 1000
  устройств их стоит отключать выше какого-то порога, иначе схема начнёт
  подтормаживать на ровном месте.

Хорошо сочетается с 2.6: цвет устройства как раз задаёт градиент рамки.

### 2.8. Вложения

- Файлы на устройстве и на связи: фото шкафа, паспорт, скан схемы.
- Хранение: локальный том или S3-совместимое (согласовать), ограничение
  размера/типов, вирус-скан вне зоны ответственности.

---

## Этап 3. Расширение модели данных

Требует отдельного согласования — меняет ядро.

### 3.1. Пассивная инфраструктура — ~~в объём не входит~~

Исключено решением заказчика. Пункт 3.1 аудита (нельзя задокументировать
линию «коммутатор → патч-панель → розетка → станок») закрывается как
«не требуется»: связь остаётся строго «порт ↔ порт».

Если потребность появится, самый дешёвый путь — завести патч-панель как
обычное устройство с портами и описать линию цепочкой связей; переработка
модели данных для этого не нужна.

### 3.2. Стойки и шкафы

- Справочник стоек (название, высота в юнитах, расположение).
- У устройства: стойка, начальный юнит, высота в юнитах (в шаблоне).
- Экран «вид стойки» спереди/сзади.

### 3.3. IPAM

- Подсети (иерархия), связь VLAN ↔ подсеть.
- Проверка конфликтов IP и попадания адреса в подсеть.
- Отчёт «занятые/свободные адреса подсети».

---

## Этап 4. Автоматизация

### 4.1. SNMP/LLDP-опрос — подтверждён заказчиком

Поля в БД (`links.source`, `confirmed`, `last_seen_at`) уже заведены — раз
опрос планируется, они остаются и в модели, и в интерфейсе (колонка
«Подтв.» на странице связей). Пункт D7 аудита закрыт этим решением.

- Фоновый воркер, опрос коммутаторов по расписанию.
- Обнаруженные связи создаются с `source='lldp'`, `confirmed=false`.
- Экран «Расхождения»: что нашлось в сети, но не задокументировано, и
  наоборот; подтверждение/отклонение оператором.
- Учётные данные SNMP — в отдельном защищённом хранилище, не в БД открыто.

### 4.2. Развёртывание `[E3]`

- `Dockerfile` для фронтенда (build → nginx), общий `docker-compose.yml`
  на весь стек, фронт и API за одним origin (снимает вопрос CORS и ввод
  адреса API руками).
- Адрес API берётся из сборки/относительного пути, поле на экране входа —
  убрать или спрятать в «дополнительно».
- Healthcheck бэкенда в compose, политика рестарта, том для вложений.
- ~~Резервное копирование БД: скрипт + инструкция в README.~~ — сделано:
  `scripts/backup.sh` / `restore.sh` и их версии для Windows.

---

## Сводная таблица приоритетов

| Этап | Содержание | Статус |
|---|---|---|
| 0 | Авторизация чтения, управление паролями, конфиг, Alembic, валидация, тесты, CI | В объёме, блокирует продуктивную эксплуатацию |
| 1 | Пагинация, топология, журнал, RBAC в UI, прочность записи + оптимистичная блокировка | В объёме, обязателен при цели в 1000 устройств |
| 2 | Импорт, карточка устройства, работа со схемой, несколько схем, цвет устройства, оформление узлов, вложения | В объёме; экспорт (2.1) исключён. Импорт (2.2) — раньше остальных, см. ниже |
| 3 | Стойки, IPAM | В объёме; патч-панели (3.1) исключены |
| 4 | SNMP/LLDP-опрос, развёртывание | В объёме, подтверждён |

## Порядок работ внутри этапа 0

Предлагаемая последовательность — так, чтобы каждый шаг был проверяемым и
ложился в отдельный обозримый PR:

1. **0.6 (каркас)** — pytest, тестовая база, GitHub Actions. Сначала рельсы,
   потом всё остальное едет по ним, а не вслепую.
2. **0.1 + 0.3** — авторизация чтения и fail-fast по конфигурации. Самое
   срочное и самое компактное по диффу.
3. **0.2** — управление учётными записями (бэкенд + экран).
4. **0.4** — Alembic и сведение `models.py` с `schema.sql`. Самый рискованный
   шаг, поэтому идёт уже при работающих тестах.
5. **0.5** — валидация типов (опирается на настоящие типы Postgres из 0.4).
6. **0.8** — изоляция площадок. Строго до наполнения базы, но не раньше: пока
   идёт отработка функционала и дизайна, ключ площадки ничего не добавляет к
   тому, что нужно увидеть, а работы в нём много.
7. **0.7** — README, удаление прототипа, замена устаревших API.

## Импорт идёт вперёд этапа 1

Формально импорт (2.2) стоит в этапе 2, но по смыслу он нужен раньше:
первичное наполнение — это первое, что произойдёт с системой после запуска,
и до него держать 1000 устройств просто негде. Порядок работ поэтому такой:

**этап 0 (включая изоляцию площадок) → импорт (2.2) → этап 1 → остаток этапа 2 → 3 → 4**

Почему не раньше этапа 0: импорт переиспользует валидацию из 0.5, а она
опирается на настоящие типы Postgres из 0.4. Делать импорт до них — значит
писать проверки дважды.

Импорт обязан идти после изоляции площадок (0.8): в файле должна быть
колонка «Площадка», иначе тысячу загруженных устройств придётся
переразмечать руками.

Оговорка по факту: облегчённый импорт (см. 2.2) сделан раньше 0.8 — он
переносит строки по одной руками, и массового наполнения без площадок им не
устроить. Когда 0.8 появится, в `import_rows` добавится колонка площадки, а
в окно переноса — её выбор.

Побочная выгода: как только импорт готов, приёмочный набор для этапа 1
(1000 устройств, 24 000 портов) генерируется им же, а не отдельным скриптом.

## Остаточные вопросы

1. ~~Журнал изменений (1.3) — доступен только `admin` или ещё и `editor`?~~ —
   решено: **доступен всем ролям**, включая `viewer`. Кто и что менял —
   рабочая информация, а не секрет.
2. ~~Коды устройств при нескольких площадках~~ — решено: нумерация сквозная
   на всю систему, SW-0042 один на свете.

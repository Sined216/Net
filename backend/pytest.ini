[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # SQLAlchemy 2.0 ругается на Query.get() в routers/catalog.py — известно,
    # заменяется на Session.get() в задаче 0.7 ТЗ.
    default::DeprecationWarning

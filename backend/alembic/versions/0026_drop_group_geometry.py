"""Снятие рамки-кластера у группы: x, y, width, height.

Группа была ещё и рамкой на схеме связей — своя область на плане со своими
координатами (заведены в 0008_group_geometry). Владелец сравнил схему с
рамками и без на живой копии и выбрал «без рамок»: рамка занимала четыре
разных места кода (сборка графа, панель действий, перетаскивание,
автораскладка) ради того, чем на практике почти не пользовались — вся эта
работа снята с фронтенда отдельным заходом раньше. Здесь снимается то, что
от неё осталось в базе: колонки, которые с тех пор не пишет и не читает
никто, и эндпоинт `PATCH /topology-groups/{id}/box`, которым их когда-то
двигали.

Группировка как данные никуда не делась: `topology_groups` и
`devices.topology_group_id` остаются как были, группа просто больше не
несёт собственной геометрии.

Накопленные координаты теряются осознанно: они относятся к рамке, которой
больше нет на экране ни в каком виде, — оставлять их значило бы хранить
мусор без единого читателя.
"""

from alembic import op
import sqlalchemy as sa

revision = "0026_drop_group_geometry"
down_revision = "0025_printer_settings"
branch_labels = None
depends_on = None

COLUMNS = ["x", "y", "width", "height"]


def upgrade() -> None:
    for name in COLUMNS:
        op.drop_column("topology_groups", name)


def downgrade() -> None:
    # Колонки вернутся пустыми: сохранённые координаты снесены вместе с
    # ними, и взять их обратно неоткуда.
    for name in COLUMNS:
        op.add_column("topology_groups", sa.Column(name, sa.Float(), nullable=True))

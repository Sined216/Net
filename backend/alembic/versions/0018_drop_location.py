"""Снятие «расположения» у устройства и у строки импорта.

Свободный текст «цех / шкаф» заведён до того, как появились группы на
топологии. Группа делает то же самое и лучше: она проверяемая (устройство
состоит ровно в одной, и та существует), вложенная (цех — участок — линия —
шкаф) и видна на схеме, а не только в списке. Два способа записать одно и
то же расходятся при первой же правке — остаётся один.

Накопленные значения теряются осознанно: перелить их в группы автоматически
нельзя (в тексте «Цех 1, шкаф 3» человек имел в виду две разные сущности,
и какие именно — знает только он), а держать колонку ради истории значит
оставить ровно ту двойственность, ради снятия которой всё и затевалось.
Решение принято явно, см. пункт 12 замечаний в docs/TZ.md.

`import_rows.location` уходит следом: это перевалочное поле, из которого
значение попадало в устройство, — без получателя оно бессмысленно. Сама
колонка файла при этом не пропадает: «цех» перестаёт быть известным
заголовком и попадает в `import_rows.extra` вместе с прочими неопознанными
столбцами, где человек видит её при переносе строки.
"""

from alembic import op
import sqlalchemy as sa

revision = "0018_drop_location"
down_revision = "0017_device_mac"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.drop_column("devices", "location")
    op.drop_column("import_rows", "location")


def downgrade() -> None:
    # Колонки вернутся пустыми: значения снесены вместе с ними, и взять их
    # обратно неоткуда.
    op.add_column("devices", sa.Column("location", sa.Text(), nullable=True))
    op.add_column("import_rows", sa.Column("location", sa.Text(), nullable=True))
